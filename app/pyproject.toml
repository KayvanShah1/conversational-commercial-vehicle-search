[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["vehicle-search-utils"]

[tool.uv.sources]
vehicle-search-utils = { workspace = true }
