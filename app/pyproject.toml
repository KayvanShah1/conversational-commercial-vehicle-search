[project]
name = "app"
version = "0.1.0"
description = "Local push-to-talk interface for the grounded vehicle-search agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "agents",
    "streamlit>=1.40.0",
]

[tool.uv.sources]
agents = { workspace = true }
