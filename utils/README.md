# Vehicle Search Utils

Shared infrastructure for workspace components. This package owns project-wide configuration such as repository paths, environment loading, and MotherDuck connection settings. Component-specific configuration remains in its owning package.